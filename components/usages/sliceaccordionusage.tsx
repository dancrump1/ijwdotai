import SliceAccordion from "@/registry/open-source/slice-accordion";

export default function Usage() {
	return (
		<div className="h-screen w-full flex items-center justify-center relative overflow-hidden bg-background">
			<SliceAccordion />
		</div>
	);
}
