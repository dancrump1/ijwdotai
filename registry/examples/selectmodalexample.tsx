"use client";

import React from "react";

import { SelectModel } from "@/components/SelectModal";

export default function Example() {
	return (
		<div className="h-screen w-full flex items-center justify-center relative overflow-hidden bg-black">
			<SelectModel />
		</div>
	);
}
